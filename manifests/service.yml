apiVersion: v1
kind: Service
metadata:
    name: "mewuaks2-9ce8"
    labels:
        app: "mewuaks2-9ce8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "mewuaks2-9ce8"